Run the project's unit tests using the Angular CLI MCP `test` tool for the `tokidev-ai-resources` project.

Report:
- How many tests passed / failed
- Any failing test names and their error messages
- If all pass, confirm with a one-line summary
