Run a production build using the Angular CLI MCP `build` tool for the `tokidev-ai-resources` project.

Report:
- Whether the build succeeded or failed
- Any TypeScript or build errors with file and line references
- Bundle size warnings if any budgets are exceeded
