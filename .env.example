# Copy to `.env` if you introduce dotenv in scripts or a local server wrapper.
# Never commit a real `.env` with secrets.

# Example placeholders when wiring APIs later:
# API_URL=http://localhost:3000
# PUBLIC_APP_NAME=tokidev
